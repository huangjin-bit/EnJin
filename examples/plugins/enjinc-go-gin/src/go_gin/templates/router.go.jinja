package router

import (
	"github.com/gin-gonic/gin"
	"{{ pkg }}/config"
)

func Setup(cfg *config.Config) *gin.Engine {
	r := gin.Default()

	r.GET("/health", func(c *gin.Context) {
		c.JSON(200, gin.H{"status": "ok"})
	})

	// TODO: Register route handlers from handler/ package

	return r
}
