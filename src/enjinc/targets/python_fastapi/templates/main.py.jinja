# ============================================================
# main.py.jinja - 应用入口模板 (基建层，非 AI 生成)
# ============================================================
# 生成 main.py 文件，FastAPI 应用启动和路由注册。

from contextlib import asynccontextmanager

from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

from app.core.config import APP_NAME, APP_VERSION
from app.core.database import engine, Base
import app.api.{{ api_version }} as api_routes  # noqa


@asynccontextmanager
async def lifespan(app: FastAPI):
    """应用生命周期管理：启动时初始化数据库，关闭时清理资源。"""
    Base.metadata.create_all(bind=engine)
    yield
    # TODO: 关闭连接池、清理缓存等


# 创建 FastAPI 应用实例
app = FastAPI(
    title="{{ app_name }}",
    version="{{ app_version }}",
    description="Generated by EnJin Compiler",
    lifespan=lifespan,
)

# 配置 CORS 中间件
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)


# ============================================================
# 注册路由
# ============================================================
from app.api.{{ api_version }} import register_all_routes
register_all_routes(app)


# ============================================================
# 健康检查端点
# ============================================================

@app.get("/health")
async def health_check():
    """健康检查端点。"""
    return {"status": "ok", "app": APP_NAME, "version": APP_VERSION}


if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host="0.0.0.0", port=8000)