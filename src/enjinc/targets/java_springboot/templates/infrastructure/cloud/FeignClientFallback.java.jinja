package {{ pkg }}.infrastructure.client;

import {{ pkg }}.interfaces.dto.response.{{ struct.name }}Response;
import org.springframework.cloud.openfeign.FallbackFactory;
import org.springframework.stereotype.Component;

import java.util.Collections;
import java.util.List;

/**
 * {{ struct.name }} Feign 降级工厂 — 服务不可用时的兜底逻辑。
 */
@Component
class {{ struct.name }}ClientFallbackFactory implements FallbackFactory<{{ struct.name }}Client> {

    @Override
    public {{ struct.name }}Client create(Throwable cause) {
        return new {{ struct.name }}Client() {
            @Override
            public {{ struct.name }}Response getById(Long id) {
                return null;
            }

            @Override
            public List<{{ struct.name }}Response> listAll() {
                return Collections.emptyList();
            }

            @Override
            public {{ struct.name }}Response create({{ struct.name }}Response request) {
                return null;
            }

            @Override
            public {{ struct.name }}Response update(Long id, {{ struct.name }}Response request) {
                return null;
            }

            @Override
            public void delete(Long id) {
                // fallback: no-op
            }
        };
    }
}
